<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title><%= content_for(:title) || "フクちゃんお悩み掲示板" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="turbo-cache-control" content="no-cache">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <!-- 🦉 Favicon設定 -->
    <link rel="icon" href="<%= asset_path('favicon.png') %>" type="image/png">
    <link rel="apple-touch-icon" href="<%= asset_path('favicon.png') %>">
    <link rel="icon" href="<%= asset_path('favicon.svg') %>" type="image/svg+xml">

    <!-- Rails 8 Propshaft対応の正しいアセット読み込み -->
<%= stylesheet_link_tag "app", data: { turbo_track: "reload" } %>
<%= stylesheet_link_tag "owls", data: { turbo_track: "reload" } %>

    <%= javascript_importmap_tags %>
  </head>

  <body class="<%= yield(:body_class) %>">
    <%= yield %>
  </body>
</html>
